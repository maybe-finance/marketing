<div class="grid grid-cols-1 p-2 bg-white border shadow-xs sm:grid-cols-3 rounded-xl">
  <aside class="bg-gray-25 p-4 rounded-lg flex flex-col gap-4 col-span-1">
    <%= render "tools/widgets/forms/roi_calculator", tool: tool, frame_id: :roi_results %>
  </aside>

  <section class="col-span-2">
    <%= turbo_frame_tag :roi_results do %>
      <div class="flex-1">
        <% if tool.blank? %>
          <%= render "tools/widgets/shared/blank_slate", text: "Fill in the fields to calculate your investment’s ROI." %>
        <% else %>
          <%= render "tools/widgets/content/roi_calculator", tool: tool %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
